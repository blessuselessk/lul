--- a/RecentFiles.qml
+++ b/RecentFiles.qml
@@ -94,8 +94,13 @@
 	}
 
 	function executeItem(item) {
-		if (item?.action)
-			Qt.openUrlExternally(item.action);
+		console.warn("[recentFiles DEBUG] executeItem called, item=" + JSON.stringify(item));
+		if (item?.action) {
+			var ok = Qt.openUrlExternally(item.action);
+			console.warn("[recentFiles DEBUG] openUrlExternally(" + item.action + ") returned " + ok);
+		} else {
+			console.warn("[recentFiles DEBUG] item.action is falsy, item=" + JSON.stringify(item));
+		}
 	}
 
 	function loadSettings() {
