--- a/Modals/DankLauncherV2/Controller.qml
+++ b/Modals/DankLauncherV2/Controller.qml
@@ -1946,6 +1946,8 @@
         if (!item)
             return;
 
+        console.warn("[launcherDEBUG] executeItem type=" + item.type + " isBuiltInLauncher=" + item.isBuiltInLauncher + " pluginId=" + item.pluginId + " data=" + JSON.stringify(item.data));
+
         SessionData.addLauncherHistory(searchQuery, explicitQuerySession);
 
         if (item.type === "plugin_browse") {
